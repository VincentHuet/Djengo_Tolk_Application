= form_for [@corresponding_phrase, @translation] do |f|
  - if @translation.errors.any?
    #error_explanation
      %h2
        = pluralize(@translation.errors.count, "error")
        prohibited this translation from being saved:
      %ul
        - @translation.errors.full_messages.each do |msg|
          %li= msg

  %br
  .field
    = t("traduction pour")
    = "»"
    %strong= Translation.where(:locale_id => 1).where(:phrase_id => @translation.phrase_id).first.text
    %br
    %br
    .span4.offset02= f.text_field :text
  .span4.offset05
    .actions
      = f.submit