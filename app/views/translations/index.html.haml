%h1 Listing translations
%table#translations-table
  %tr.bottom_border
    %td
      %strong 
        #normal Translation
    %td
      %strong 
        #skinny Translator
    %td
      %strong 
        #normal English Value
    %td
      %strong
        #skinny Locale
    %td
      %strong
        #skinny status
    %td
      %strong 
  - @translations.each do |translation|
    %tr
      %td= translation.text 
      %td= Translator.find(translation.translator_id).name
      /%td= Locale.find(translation.locale_id)
      - current_locale = Locale.new 
      - current_locale = current_locale.primary_locale
      %td= current_locale.translations.where(:phrase_id => translation.phrase_id).first.text
      %td= Locale.find(translation.locale_id).name
      %td= translation.needed_update
      - if translation.need_updated?(current_translator)
        %td= link_to 'Edit', edit_translation_path(translation)
/ %hr
/ %table#translations-table
/   %tr.bottom_border
/     %td
/       %strong 
/         #normal Path
/     %td
/       %strong 
/         #normal Keys table
        
/   - @translations.each do |translation|
/     %tr
/       /- translation.phrases.yaml_path
/       %td= translation.phrase.yaml_path.gsub!(/[.]{2}/,'')
/       %td= translation.phrase.yaml_path.to_s.split('.')
  
%br